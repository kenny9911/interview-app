# 56 · Implementation Review #2 (avg 77/100, was 68)

Blockers:
- [Concurrency / D2 reconciliation] server/src/app.ts:149-178 — Add a version-checked saveStateIfVersion(state, expectedVersion) to the Store interface; have the reviewer apply only a narrow delta via a read-modify-CAS retry loop instead of saveState(fresh). Never overwrite whole state from the async path. Deep-copy (structuredClone) on MemoryStore read/write so callers cannot mutate stored state by reference.
- [Security / AuthN+AuthZ] server/src/app.ts:69-228, server/src/livekit/token.ts:16-32 — Add a Fastify auth preHandler that verifies the access JWT (JWT_SECRET already in env) and resolves userId from the token, not the body. On every /sessions/:id route, load the session and assert it belongs to the authenticated caller before acting. Drop userId from the /token body and derive identity from the verified principal.
- [D2 staleness bound (dead check)] server/src/agents.ts:166,173-179 — Capture the answered turn's own version and set basedOnVersion to that immutable value; reconcile against fresh.version read inside the CAS loop. Add a unit test that advances the cursor two questions while a review is in flight and asserts the patch is dropped or re-targeted.
- [Consent / D9 egress TOCTOU (server)] server/src/app.ts:69-103, agent/src/agent.ts:33-108 — Persist a versioned ConsentRecord and gate any recording/egress start behind a server-side re-read of the latest recording consent at participant_joined. Add the hook + check now so enabling egress later cannot bypass it.
- [Error handling / information disclosure] server/src/app.ts:49-52 — Return a generic {error:'internal'} body in production and log full detail server-side only. Map known LLM error classes (429/5xx/timeout) to 502/503 with Retry-After. Wrap LLM calls in an AbortController timeout and surface 504/503.
- [Voice / turn serialization + barge-in] agent/src/agent.ts:78-99 — Serialize turns with an in-flight guard (ignore/queue new finals while a nextTurn is pending) and drive the audio loop through the tested TurnLoop, or assert in a test that overlapping finals cannot double-post. Wire explicit cancel_tts on barge-in.
- [Live / Orb state machine (D7)] /Users/kenny/code/interview-app/mobile/src/components/Orb.tsx, InterviewOrb.tsx, livekit/room.ts, screens/LiveScreen.tsx — Thread level through LiveScreen->InterviewOrb->Orb; drive the listening Ring scale/opacity from an Animated.Value fed by level (listening only) and voiceBars scaleY from level (speaking only). Render listening as rings-without-bars and speaking as bars-with-a-steady-core so the states are visually distinct.
- [Live / interrupted state (D7)] /Users/kenny/code/interview-app/mobile/src/components/InterviewOrb.tsx — Add a distinct transient treatment for orbState==='interrupted' (a one-shot scale contract + flash via an Animated sequence) before transitioning to listening, with its own accessibility announcement ('Interrupted').
- [Consent gate / OS permissions + text-only fallback (D9)] /Users/kenny/code/interview-app/mobile/src/screens/ConsentScreen.tsx, src/livekit/room.ts:106 — On Consent continue, request OS mic (and camera if toggled) permission before starting the session; if denied, show a blocking state with Linking.openSettings() AND a 'Continue in text-only mode' button that routes into the same Live pipeline via the /next-turn text path. Stop relying on a regex against room.error for the primary permission UX.
- [Accessibility] /Users/kenny/code/interview-app/mobile/src/components/ui.tsx, screens/*.tsx — Add accessibilityRole='button' + descriptive labels/state to PrimaryButton, BackButton, the four Live controls, and the consent Toggle (role='switch', checked state). Gate orb/ring/glow/equalizer loops behind AccessibilityInfo.isReduceMotionEnabled with a static fallback. Make the captions rail an accessibilityLiveRegion.
- [Live / captions surface (D10)] /Users/kenny/code/interview-app/mobile/src/screens/LiveScreen.tsx, livekit/room.ts — Extend the caption data-channel message to carry {speaker, isFinal}; keep a short rolling list of segments in the hook; render a scrollable rail that styles partials dim/italic and finals solid, tags each line You/persona, and auto-scrolls to the latest.
- [Persona/style differentiation (live)] server/src/app.ts:93-96, server/src/livekit/token.ts:16-32, agent/src/agent.ts:40 — On session-create, create the room via RoomServiceClient with metadata JSON {style, persona} (or set it on the agent-dispatch RoomConfiguration); in agent.ts map persona->a distinct Cartesia voice id passed into the TTS constructor. Add a test asserting metadata round-trips and that each persona yields a different voice id.
- [Agent dispatch] server/src/app.ts:69-103, server/src/livekit/token.ts — Use explicit dispatch: set RoomConfiguration.agents on the user token (or call AgentDispatchClient) in /v1/sessions so the named agent is guaranteed to join. Add a client-side watchdog in LiveScreen (e.g. 15s without 'live') that surfaces a recoverable error + retry instead of an indefinite spinner.
- [Scoring-fairness / calibration gate (D12)] /Users/kenny/code/interview-app/server/tests/agents.test.ts (and absent eval harness) — Add a golden-transcript calibration suite (8-12 hand-scored transcripts asserting in-band; a verbosity-bias pair test asserting the concise answer scores >= the padded one; an N-run self-consistency test asserting overallScore stddev under a documented threshold) and wire it into the phase-done >=90 gate (D15).
- [Scoring-fairness / style leakage] /Users/kenny/code/interview-app/server/src/prompts/agents.ts:132 — Drop `Style: ${c.style}` from analystPrompts, or add an explicit guard line ('Interview style affects how questions were asked, NOT the scoring bar — score identical content identically regardless of style'). Add a calibration test asserting the same transcript scores within tolerance across all three styles.
- [Scoring-calibration / aggregation] /Users/kenny/code/interview-app/server/src/prompts/agents.ts:135-137 — Specify aggregation in the prompt ('overallScore should reflect a weighted blend of the four competencies; do not deviate from their mean by more than ~10 unless justified') and/or compute overallScore server-side from competencyScores as the source of truth, then bandFor on that. Add a test asserting |overall - weightedMean(competencies)| is bounded.